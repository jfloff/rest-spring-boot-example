
version: '3'
services:
    server:
        build: ./server
        environment:
            - SERVER_OF=client
            - THREAD_FACTOR=2

    client:
        build: ./client
        environment:
            - CLIENT_OF=server
            - CLIENT_THREADS=2
